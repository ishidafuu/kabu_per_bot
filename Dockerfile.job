FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml /app/pyproject.toml
COPY src /app/src
COPY scripts /app/scripts

RUN pip install --no-cache-dir --upgrade pip && pip install .

ENV PYTHONPATH=/app/src

CMD ["python", "scripts/run_daily_job.py", "--stdout"]
